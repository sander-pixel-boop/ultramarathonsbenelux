Learned that `JSON.parse()` natively handles unescaping of unicode sequences, so manually chaining `.replace()` calls to format characters like `\u003c` back to `<` is unnecessary. Removing these string manipulations improves code health and reduces overhead, particularly in test environments where avoiding dependencies like `jsdom` just to test text formatting helps speed up execution.
