--- utils/date.js
+++ utils/date.js
@@ -9,7 +9,8 @@
     const str = String(dateStr).trim();

     if (parseStandardDateMemo.has(str)) {
-        return parseStandardDateMemo.get(str);
+        const cached = parseStandardDateMemo.get(str);
+        return cached ? { ...cached } : null;
     }

     let result = null;
